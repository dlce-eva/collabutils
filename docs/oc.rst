
OwnCloud
========

.. note::

    This functionality is available, when `collabutils` is installed with the
    `owncloud` extra, i.e. via

    .. code-block:: shell

        pip install collabutils[owncloud]


.. automodule:: collabutils.oc
    :members:

